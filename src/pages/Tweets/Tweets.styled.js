import { styled } from "@mui/material";

export const TweetsPageContainer = styled("div")({
  padding: 16,
});

export const TweetsPageContent = styled("div")({
  display: "flex",
  flexDirection: "column",
  gap: 16,
});

export const TweetsPageActivityWrap = styled("div")({
  display: "flex",
  justifyContent: "space-between",
});
